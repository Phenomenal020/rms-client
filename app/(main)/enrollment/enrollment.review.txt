================================================================================
ENROLLMENT ROUTE — CODE REVIEW
Route: client/app/(main)/enrollment
Reviewed: 2026-06-08
Files: page.tsx, enrollment-form.tsx, edit-enrollment-modal.tsx,
       loading.tsx, enrollment-loading-table.tsx
Related: fetcher/queries.ts, fetcher/mutations.ts, types/enrollments.ts,
         types/students.ts (enrollmentPayload), api/src/students/*,
         sidebar.tsx, middleware.ts, shared-components/org-admin-gate.tsx
Review indicator:
 ✅ - FIXED
 📌 - SIGNIFICANT CHANGE/API CHANGE. To be done later
================================================================================


1) LOADING INDICATION — Suspense vs loading.tsx (with skeletons)
--------------------------------------------------------------------------------

Previous state
  • No loading.tsx; page.tsx showed plain "Loading terms..." text.
  • Student fetch used animate-pulse text, not table skeleton.
  • page.tsx blocked entire route on getTerms() before rendering form.

Recommendation: use BOTH layers (same pattern as classes/students).

  A) loading.tsx (route-level) + OrgAdminGate fallback
     FIX: EnrollmentLoading with main shell + class picker skeleton + table ✅

  B) In-component skeleton (data-level)
     • isLoadingTerms → EnrollmentLoadingTable
     • selectedClassId + isLoadingStudents → EnrollmentLoadingTable
     FIX: Implemented as recommended ✅

  C) Suspense — not used (client SWR pattern) ✅

  D) Mutation loading — LoadingButton on Save Enrollment
     FIX: Already handled ✅

  E) Term/active-term UX moved into EnrollmentForm
     page.tsx no longer gates with plain divs; form shows dashed banners + skeletons.
     FIX: Implemented ✅


2) API ISSUES (current + potential)
--------------------------------------------------------------------------------

Critical / high

  [Wrong SWR mutate key after save]
    saveEnrollmentHandler called mutate(`/api/v1/students/${selectedClassId}`) but
    getEnrollments key is `/api/v1/students/enrollments?classId=...&termId=...`.
    Table did not refresh after save until manual navigation.
    FIX: mutate uses correct enrollments URL ✅

  [GET errors masked as empty data]
    getEnrollments / getSubjectClassAssignments return [] on failure → empty states.
    FIX: termsError, classesError, enrollmentsError surfaced via ErrorBanner ✅

  [selectedClass fallback to classes[0]]
    When selectedClassId set but not found, code used classes[0] for assignments —
    wrong modal subjects for unrelated class.
    FIX: selectedClass null-safe; assignments = selectedClass?.assignments ?? [] ✅

  [enrolledLabel subjectId vs assignmentId]
    enrolledLabel mapped enrolledSubjectIds (assignmentIds) with
    classAssignments.find(a => a.subjectId === id) — incorrect join.
    Function removed (was unused in table render); modal uses assignmentId correctly.
    FIX: Dead/wrong helper removed ✅

  [useSaveEnrollment no onSuccess cache invalidation]
    Relies on manual mutate in handler — now uses correct key.
    FIX: Implemented in saveEnrollmentHandler ✅

  [Middleware matcher gap]
    /enrollment in protectedRoutes but matcher not wired (shared).
    FIX: Added /enrollment to protectedRoutes ✅

Medium

  [page.tsx was client + duplicate term fetch]
    Term resolution split across page and form; plain-text loading states.
    FIX: Server page + OrgAdminGate; terms fetched inside EnrollmentForm ✅

  [console.log noise]
    enrollment-form logged classes and enrollmentsData.
    FIX: Removed ✅

  [console.log in useSaveEnrollment]
    Still in fetcher/mutations.ts.
    FIX: Removed ✅

  [No active term]
    Previously plain "No active term found" div on page.
    FIX: In-card dashed banner with actionable copy ✅

  [Checkbox stores assignmentId; API expects enrolledSubjectIds]
    Names align with SaveEnrollmentDto.enrolledSubjectIds (assignment UUIDs) ✅


3) ERROR HANDLING — 400, 500, client behaviour, backend mitigation
--------------------------------------------------------------------------------

  • getApiErrorMessage + handleAuthRedirect on save (mirrors classes/students).
  • termsError → critical ErrorBanner; blocks class picker flow.
  • enrollmentsError → banner when class selected.
  • classesError → auxiliary banner; table can still show if enrollments OK.
  • useEffect 401 → /sign-in, 403 → /forbidden on fetch errors.
  FIX: Implemented as recommended ✅

  | Scenario                    | After fix                    |
  |-----------------------------|------------------------------|
  | Terms load fail             | ErrorBanner + retry          | ✅
  | No ACTIVE term              | Dashed in-card message       | ✅
  | Enrollments GET fail        | ErrorBanner (not empty list) | ✅
  | Save 400 invalid subject id | Toast via getApiErrorMessage | ✅
  | Save 401/403                | Redirect                     | ✅
  | Class with zero students    | Empty state (no error)       | ✅

Backend (students.service saveEnrollment / getEnrollments)
  ✓ OrgAdminGuard, requireOrganizationId
  ✓ Validates assignment ids belong to class/term
  📌 Return updated enrollment row on POST for optimistic UI


4) HIDE ROUTE FROM NON-ORG USERS + DISABLE ACTIONS FOR NON-ORGADMIN
--------------------------------------------------------------------------------

  A) Sidebar — Enrollment under orgAdminTermItems ✅
  B) OrgAdminGate on page.tsx + EnrollmentLoading fallback
     FIX: Implemented ✅
  C) canManage — hide Edit column, readOnly modal, early returns in handlers
     FIX: Implemented ✅
  D) OrgAdminGate !isOrgAdmin fallback — shared fix from school route ✅


5) PROTECTED ROUTE
--------------------------------------------------------------------------------

  1. Middleware matcher FIXED: ✅
  2. OrgAdminGate on page ✅
  3. OrgAdminGuard on StudentsController (enrollment endpoints) ✅
  4. Optional org-admin layout [ToDo...] 📌


6) STRUCTURE, LAYOUT, CODE SMELLS
--------------------------------------------------------------------------------

What works well
  • Class picker Popover + search (good UX for many classes)
  • Edit modal checkbox grid for subject assignments
  • Term-scoped data via activeTermId
  • enrollmentPayload mapping to EnrollmentStudent

Code smells / improvements

  • enrollment-form.tsx ~350 lines — extract useEnrollmentPageData later 📌
  • Unused imports removed (getClassPayload, studentPayload, etc.) ✅
  • Table shows subject count badge not names (OK for density; tooltip could help) 📌
  • getEnrollments returns untyped data: [] — generic hook 📌
















7) TEST CASES
--------------------------------------------------------------------------------

  High
  • Terms loading → EnrollmentLoadingTable
  • No active term → banner (not blank page)
  • Select class → fetches enrollments with classId + termId query
  • Save enrollment → mutates correct SWR key; table updates
  • enrollmentsError → ErrorBanner not "no students"
  • Edit hidden when !canManage
  • 401/403 on GET → redirect

  Medium
  • Class picker search filters list
  • Modal pre-checks student.enrolledSubjectIds
  • Empty class assignments → modal message, save disabled
  • Changing class clears edit modal state

  Edge
  • Save with no class / no active term → toast block
  • Concurrent save two students
  • Class selected but zero students in class


8) TYPESCRIPT COMMENTARY
--------------------------------------------------------------------------------

  • EnrollmentStudent exported from enrollment-form for modal ✅
  • subjectClassAssignmentPayload casts — fix hook generics 📌
  • enrollmentPayload imported from types/students (cross-module) 📌
  • SaveEnrollmentPayload in types/enrollments.ts ✅


9) GET REQUEST DTOs
--------------------------------------------------------------------------------

  getTerms()
    Derives activeTermId for all downstream fetches ✅

  getSubjectClassAssignments(activeTermId)
    GET /api/v1/classes/enrollments?termId=
    Response: subjectClassAssignmentPayload[] with assignments[]
    FIX: classesError surfaced ✅

  getEnrollments(classId, termId)
    GET /api/v1/students/enrollments?classId=&termId=
    Response: enrollmentPayload[] (from students.ts)
    FIX: enrollmentsError surfaced; [] only when success + empty ✅


10) MUTATION DTOs
--------------------------------------------------------------------------------

  SaveEnrollmentPayload
    { studentId, enrolledSubjectIds, activeTermId }
    Maps to POST /api/v1/students/enrollment body (SaveEnrollmentDto) ✅

  Client sends assignment UUIDs in enrolledSubjectIds — matches backend ✅

  Gaps:
    • useSaveEnrollment should onSuccess mutate enrollments key in hook 📌
      (manual mutate in form is OK for now) ✅ partial


11) API RECOMMENDATIONS
--------------------------------------------------------------------------------

  • Add onSuccess to useSaveEnrollment with enrollments key matcher
  • Return saved enrollment ids from POST for confirmation
  • Include subject names in GET enrollments to avoid client join
  • 409 for conflicting enrollment rules if added later
  • Remove console.log from useSaveEnrollment


================================================================================
QUICK WINS (priority order)
================================================================================
1. loading.tsx + EnrollmentLoadingTable + in-component skeletons
   FIX: Implemented ✅
2. Fix SWR mutate key after save
   FIX: Implemented ✅
3. Surface terms/classes/enrollments errors
   FIX: Implemented ✅
4. OrgAdminGate + canManage gates
   FIX: Implemented ✅
5. getApiErrorMessage + handleAuthRedirect on save
   FIX: Implemented ✅
6. Move term logic from page into form; remove plain-text loading
   FIX: Implemented ✅
7. Fix selectedClass classes[0] fallback
   FIX: Implemented ✅
8. Remove console.log from enrollment-form
   FIX: Implemented ✅
9. Middleware matcher [ToDo... shared] 📌
10. useSaveEnrollment onSuccess in mutations.ts [ToDo... fetcher] 📌

================================================================================
END OF REVIEW
================================================================================
